services:
  - type: web
    name: my-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app  # or uvicorn main:app --host 0.0.0.0 --port 10000
    branch: main
    plan: free
